 {
     "builds": [
         {
             "src": "mnist_api/main.py",
             "use": "@vercel/python"
         }
     ],
     "routes": [
         {
             "src": "/(.*)",
             "dest": "/"
         }
     ],
      "installCommand": "pip install -r requirements.txt"
 }
